package routes

import (
	"github.com/gin-gonic/gin"
)

//SetupRouter - setup web-server instance with routes
func SetupRouter() *gin.Engine {
	r := gin.Default()
	// v1 := r.Group("/v1")
	// {
	// }
	return r
}
